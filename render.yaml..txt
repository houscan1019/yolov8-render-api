services:
  - type: web
    name: yolov8-deskew-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    # This port must match the port your Flask app listens on
    # In main.py, we have app.run(..., port=5000), so we use 5000 here.
    port: 5000
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.12 # Or any other specific Python version you prefer (e.g., 3.9.18, 3.11.8)